import {ProjectReport} from '../types';
export function reportToMarkdown(r:ProjectReport){return `# ${r.projectSummary.title}\n\n> ${r.projectSummary.verdict}\n\n- 综合评分：${r.projectSummary.score}/100\n- 推荐策略：${r.strategy.type}（${r.strategy.confidence}%）\n- 分析时间：${new Date(r.generatedAt).toLocaleString('zh-CN')}\n\n## 推荐组合\n${r.strategy.recipe.map(x=>`- ${x}`).join('\n')}\n\n## Agent\n${r.agents.map(x=>`- ${x.name}：${x.role}，匹配度 ${x.matchScore}%`).join('\n')}\n\n## 模型\n${r.models.map(x=>`- ${x.name}（${x.modelId}）：${x.task}，匹配度 ${x.matchScore}%`).join('\n')}\n\n## GitHub 参考\n${r.githubProjects.map(x=>`- [${x.name}](${x.url})：${x.recommendedUse}`).join('\n')}\n\n## 技术栈\n${r.techStack.map(x=>`- ${x.layer}：${x.name} — ${x.reasons.join('；')}`).join('\n')}\n\n## 工作流\n${r.workflows.map((x,i)=>`${i+1}. ${x.title}：${x.output}`).join('\n')}\n\n## 估算\n- Token：${r.estimates.tokens.display}（${r.estimates.tokens.range}）\n- 时间：${r.estimates.time.display}\n- 成本：${r.estimates.cost.display}\n- 自动化率：${r.estimates.automation.rate}%\n\n## 验收标准\n${r.projectSummary.acceptanceCriteria.map(x=>`- [ ] ${x}`).join('\n')}\n`}
export function downloadText(filename:string,text:string,type:string){if(typeof document==='undefined')throw new Error('下载功能只能在浏览器中使用');const blob=new Blob([text],{type}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename;a.rel='noopener';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
