import {Project} from '../types';import {detectProjectKind} from '../data/questions';import {buildMockReport} from '../data/reportCatalog';export async function createProjectRecord(idea:string):Promise<Project>{return{id:crypto.randomUUID(),idea,kind:detectProjectKind(idea),evaluationMode:'quick',createdAt:new Date().toISOString()}}export function getRequirementPreview(project:Project){return buildMockReport(project).projectSummary}
