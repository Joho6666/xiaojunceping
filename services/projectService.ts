import {Project} from '../types';import {detectProjectKind} from '../data/questions';import {extractRequirementProfile} from './requirementExtractionService';
export async function createProjectRecord(idea:string):Promise<Project>{return{id:crypto.randomUUID(),idea,kind:detectProjectKind(idea),evaluationMode:'quick',createdAt:new Date().toISOString()}}
export function getRequirementPreview(project:Project){const profile=extractRequirementProfile(project,{});return {title:project.idea.slice(0,36)||'未命名项目',typeLabel:profile.domain?.join(' / ')||project.kind,summary:project.idea,verdict:'需求预览：完成分析后生成结论',score:0,status:'待分析',acceptanceCriteria:profile.acceptanceCriteria||[]}}
