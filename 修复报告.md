# 修复报告

日期：2026-08-16
项目：xiaojunceping（AgentScope 原型 / Joho6666/xiaojunceping）
范围：SQLITE 主键冲突修复 + 全代码库 bug 检测（竞态 / 错误处理 / 安全 / 类型 / 边界）+ P0/P1 修复

---

## 一、原始问题：SQLITE_CONSTRAINT_PRIMARYKEY

### 根因

`services/knowledgeBaseService.ts` 的 seed 数组由 5 个来源拼接（baseSeed + expandedKnowledgeCatalog + additionalKnowledgeCatalog + modelKnowledgeCatalog + knowledgeCatalogExpansion），其中 **2 个 id 重复**：

| 重复 id | 位置 A | 位置 B |
| --- | --- | --- |
| `mcp-brave-search` | `data/knowledgeCatalog.ts:45` | `data/knowledgeCatalogExpansion.ts:59` |
| `plugin-github-copilot` | `data/knowledgeCatalog.ts:51` | `data/knowledgeCatalogExpansion.ts:60` |

`seedIfEmpty` 使用裸 `INSERT`（无 ON CONFLICT），在**全新数据库**初始化时第二条重复记录直接抛 `UNIQUE constraint failed: knowledge_items.id`。本地 `.agentscope/knowledge.sqlite` 已有数据，`seedIfEmpty` 提前返回，所以现有测试"碰巧"通过——失败只在干净环境（CI / 新克隆 / 删除 .agentscope）触发，已用临时目录全新 DB 复现确认。

### 修复（双层防护）

1. **数据层去重**：`data/knowledgeCatalogExpansion.ts` 删除 2 条重复条目（保留 `knowledgeCatalog.ts` 中字段更全的版本，知识不丢失）。
2. **代码层防御**：`services/knowledgeBaseService.ts`
   - seed 数组构建时按 id 去重（保留首个出现，`uniqueSeed`）；
   - `seedIfEmpty` 的 INSERT 改为 `ON CONFLICT(id) DO UPDATE`（与 `upsertKnowledgeItem` 同语义）。

### 验证

- `npx tsx` 校验：112 条 seed 条目，112 个唯一 id，0 重复；
- 全新数据库（临时目录）初始化成功：`FRESH-DB OK: 112 items, 112 unique ids`（修复前同场景抛主键冲突）。

---

## 二、全面 bug 检测结果

检测范围：app/（页面 + 全部 API 路由）、components/、services/、ai/、data/、scripts/，约 6400 行。SQL 注入与凭据泄露方向未发现可利用问题：所有 SQLite 访问均为参数化绑定；凭据仅存服务端内存 + AES-256-GCM 加密落盘（需 `DATABASE_ENCRYPTION_KEY`）；API 只返回 `maskedKey`；localStorage 无明文密钥。

### P0（1 项，已修复）

| # | 位置 | 问题 | 状态 |
| --- | --- | --- | --- |
| 1 | `ai/cli/cliAdapter.ts:8`（链路：`app/api/connections/provider/[provider]/cli/check/route.ts:2` → `services/connectionService.ts:174` → `ai/cli/codexCliAdapter.ts:51`） | **Windows 命令注入**。`runCLI` 在 win32 下 `spawn(command, args, { shell: true })`，Node 不会转义 args，参数原样拼进 cmd.exe 命令行。`--model` 值来自 API 请求体，而 `connectionService.checkProviderCLI` 的模型白名单校验仅在 `status.available && status.authenticated` 时执行——Codex 已安装但未登录时（available: true, authenticated: false）任意字符串入库并在后续分析时进入命令行，`& calc &` 等 metachar 直接执行 | ✅ 已修 |

**修复方式（fail closed，双层）**：
- `ai/cli/cliAdapter.ts`：`runCLI` 入口新增 `assertSafeArgs`，所有命令行参数必须匹配白名单 `/^[A-Za-z0-9_@+=.,:/\\-]+$/`（不含空格、`&|<>^%` 等 metachar，`%` 亦排除以防 cmd 变量展开），否则在 spawn 之前同步抛 `CLI_UNSAFE_ARGUMENT`。prompt 走 stdin 不受影响。这是最终防线，覆盖所有现在和未来的调用路径。
- `services/connectionService.ts`：`checkProviderCLI` 的 model 校验不再依赖登录状态——只要提供了 model 就先做格式校验 `/^[\w.-]+$/`，再尝试 `listCodexModels()` 成员校验（列表非空时）；无法校验即拒绝。
- 已验证：`npx tsx` 注入测试中 `--model "gpt-5.1 & calc & rem"` 被同步拒绝，不产生进程。

### P1（7 项，全部修复）

| # | 位置 | 问题 | 状态 |
| --- | --- | --- | --- |
| 1 | `ai/cli/cliAdapter.ts:8` | `child.stdin` 无 `error` 监听：CLI 缺失或提前退出时 stdin 写入抛 EPIPE/ERR_STREAM_DESTROYED 成为未捕获异常，可崩溃 Node 进程 | ✅ 加 `child.stdin.on('error')` + write 同步 try/catch，统一走 `finish(error)` |
| 2 | `ai/cli/cliAdapter.ts:8` | shell 模式下超时/超限 `child.kill()` 只杀 cmd.exe 包装进程，真正的 codex 孙进程成为孤儿，300s 长分析反复超时即进程泄漏 | ✅ 新增 `killTree`：win32+shell 时用 `taskkill /pid <pid> /T /F` 杀进程树 |
| 3 | `services/connectionService.ts:174` | model 白名单校验依赖 `authenticated`（P0 前半链，见上） | ✅ 与 P0 一并修复 |
| 4 | `app/settings/ai/page.tsx:45-60,104-134,150-163` | `load` / `saveCapabilityConfig` / `testCapability` / `connectKey` / `oauth` / `remove` / `removeCapability` 均无 try/catch：网络错误或响应非 JSON 时 `setBusy("")` 永不执行，按钮永久"保存中…"禁用，且产生 unhandled rejection | ✅ 全部补 try/catch/finally，busy 状态必复位，失败给出提示文案 |
| 5 | `components/report/ReportSections.tsx:94-101` | `r.alternatives[s]` 无空防护：旧版/畸形持久化报告 `alternatives` 为空数组时索引访问 undefined → `TypeError` 整页崩溃（ErrorBoundary 兜底但页面不可用） | ✅ 改为 `r.alternatives[s] ?? r.alternatives[0]` + 条件渲染 |
| 6 | `components/report/ReportSections.tsx:578` | `x.breakdown.map`：部分旧报告 estimate 对象缺 `breakdown` 数组时 `.map` 崩溃（report/page.tsx 注释自认该场景真实存在） | ✅ `(x.breakdown \|\| []).map` |
| 7 | `components/AppProvider.tsx:33` + `app/history/page.tsx:37` | localStorage 历史条目只校验 `project.id/idea` 不校验 `kind`（主状态恢复路径有 kind 白名单，历史路径没有），畸形缓存导致 history 页 `kind.toUpperCase()` 崩溃 | ✅ 恢复历史时按同一 kind 白名单归一化为 `general` |

### P2（12 项：6 项防崩溃类已顺手修复，6 项记录待办）

已修复（均为单行防御，成本低收益高）：

| # | 位置 | 问题 | 修复 |
| --- | --- | --- | --- |
| 1 | `components/report/ReportSections.tsx:315` | `ratings` 值 >5 时 `"☆".repeat(5 - v)` 负数参数抛 `RangeError` | clamp 到 0–5 |
| 2 | `components/report/ReportSections.tsx:789` | `pricingLevel` 为负时 `"$".repeat()` 抛 `RangeError` | clamp 到 0–5 |
| 3 | `services/requirementExtractionService.ts:18` | `byKind[project.kind]` 无兜底，API 透传非法 kind 时 `base.capabilities` 抛 TypeError（路由兜成误导性 502） | `\|\| byKind.general` |
| 4 | `services/ecosystemDiscoveryService.ts:202` | 同类：`[...byKind[project.kind], ...]` 展开 undefined | `\|\| byKind.general` |
| 5 | `services/interviewService.ts` | `common[project.kind].map` 无兜底 | `\|\| common.general` |
| 6 | `data/questions.ts` | `getQuestions` 的 `dynamic[kind]` 同类无兜底 | `\|\| dynamic.general` |

未修复（记录，建议后续迭代处理；均无现实触发路径或影响有限）：

| # | 位置 | 问题 | 分级理由 |
| --- | --- | --- | --- |
| 1 | `app/api/knowledge/import/route.ts:10` | 知识导入只校验 `sourceUrl` 以 http 开头，`url` 字段未校验协议（潜在存储型 XSS） | 当前不可利用：导入强制 `publication: "pending"` 不参与推荐，且 `listKnowledgeItems` 过滤 `source_url LIKE 'http%'`；一旦出现"确认发布"入口即成真漏洞，届时需加协议白名单 |
| 2 | `components/report/ReportSections.tsx:183,238,412,736` | 知识条目 `url` 直接渲染 `href`，无协议白名单（React 不拦 `javascript:`） | 同上，数据源已被上游过滤，纵深防御不足 |
| 3 | `app/project/[id]/report/page.tsx:72` | `app.setReport({ ...app.report!, ...data })` 非空断言：await 期间报告可能已被"重新分析"清空，写入不完整对象 | 需重构状态流，短期有 ErrorBoundary 兜底 |
| 4 | `services/reportService.ts:4,11` | 浏览器端 fetch 无超时，后端挂起时 UI 无限等待 | 本地工具，影响小 |
| 5 | `app/page.tsx:54-61`、`app/project/[id]/report/page.tsx:51-73`、`app/project/[id]/analysis/page.tsx:61-71` | 多处 `.catch(() => undefined)` 吞错静默；首页 deps 含 `selectedConnectionId` 导致切换模型重复拉取连接列表 | 体验问题，无崩溃 |
| 6 | `services/localCapabilityDiscoveryService.ts:55` | `readdirSync` 在 `existsSync` 之后仍可抛 EACCES/EBUSY（Windows 目录被占用），discover-local 路由无 try/catch → 500 | 非常态路径 |
| 7 | `services/ecosystemDiscoveryService.ts:93` | 空 catch 吞掉 GitHub 实时搜索全部异常，故障不可观测 | 可观测性问题 |
| 8 | `services/modelCatalogSyncService.ts:17` | Gemini key 走 URL query `?key=`（官方 API 设计），自定义代理BaseUrl 时可能被访问日志记录 | 官方用法，记录风险 |
| 9 | `app/api/projects/[id]/analyze/route.ts:9` 等路由 | 请求体无大小限制（App Router 无内置限制） | 本地工具风险低 |
| 10 | `.agentscope/reports.sqlite` | 遗留文件，无任何代码引用（reportStore 实际用 knowledge.sqlite） | 可手动删除 |
| 11 | `services/connectionService.ts:172-218` | `checkProviderCLI` 并发调用同一 provider 时可能各自创建 CLI 连接条目（existing 检查在 await 之后） | 只产生重复条目，无数据损坏 |
| 12 | `services/knowledgeBaseService.ts` | 模块级 DB 连接从不关闭；`seedMissing` 每次冷启动执行约 100 次 upsert | 本地单进程场景影响小 |

### 明确排除的误报方向

- `deepseekService` / `githubService` / `browserSearchService`：超时（AbortController/AbortSignal.timeout）、`response.ok` 检查、`encodeURIComponent`、github.com URL 正则白名单（防 SSRF）、key 脱敏（sk-/ark- 模式）均到位。
- `generateDeepSeekEvaluation` 重试链：safeRetry/formatRetry 各只重试一次，无无限递归。
- better-sqlite3 单例：模块级 `db` 惰性初始化 + 同步 API，请求间无交错，无竞态。
- `localCapabilityDiscoveryService`：使用 `execFile` + 参数数组（无 shell 拼接），是安全写法。

---

## 三、修改文件清单（11 个文件）

| 文件 | 改动 |
| --- | --- |
| `data/knowledgeCatalogExpansion.ts` | 删除与主目录重复的 `mcp-brave-search`、`plugin-github-copilot` 两条目（主键冲突根因） |
| `services/knowledgeBaseService.ts` | seed 按 id 去重（`uniqueSeed`）；`seedIfEmpty` INSERT 改 `ON CONFLICT(id) DO UPDATE`（防未来目录重复引入） |
| `ai/cli/cliAdapter.ts` | `runCLI` 新增 `assertSafeArgs` 参数白名单（P0 命令注入根防御）；新增 `killTree`（taskkill /T 进程树清理）；`child.stdin` 加 error 监听 + write try/catch（EPIPE 防崩溃） |
| `services/connectionService.ts` | `checkProviderCLI` 的 model 校验不再依赖 `authenticated`，格式白名单 `/^[\w.-]+$/` + 模型列表成员校验，无法校验即拒绝（P0 前半链） |
| `app/settings/ai/page.tsx` | `load`/`saveCapabilityConfig`/`testCapability`/`connectKey`/`oauth`/`remove`/`removeCapability` 补 try/catch/finally，busy 必复位，失败有提示 |
| `components/report/ReportSections.tsx` | `alternatives[s]` 空防护（条件渲染）；`breakdown` 缺失防护；`"★"/"☆"` repeat clamp 0–5；`"$".repeat` clamp 0–5 |
| `components/AppProvider.tsx` | 历史记录恢复时 project.kind 白名单归一化（与主状态恢复一致） |
| `services/requirementExtractionService.ts` | `byKind[project.kind]` 兜底 `byKind.general` |
| `services/ecosystemDiscoveryService.ts` | `byKind[project.kind]` 兜底 `byKind.general` |
| `services/interviewService.ts` | `common[project.kind]` 兜底 `common.general` |
| `data/questions.ts` | `dynamic[kind]` 兜底 `dynamic.general` |

约束遵守情况：TypeScript 严格模式兼容（`tsc --noEmit` 通过）；未引入任何新依赖（taskkill 为 Windows 系统命令，经 `child_process.spawn` 调用）。

---

## 四、测试结果（全部通过）

| 验证项 | 结果 |
| --- | --- |
| `npm run test:reports` | ✅ 5 类项目报告数据隔离与结构校验通过 |
| `npm run test:knowledge` | ✅ knowledge tests passed |
| `npm run test:knowledge-coverage` | ✅ 126 entries, 18 media-capable models |
| `npm run test:local-discovery` | ✅ 0 local capabilities, no sensitive fields |
| `npm run test:customization` | ✅ customization tests passed |
| `npm run test:prompt` | ✅ prompt tests passed |
| `npm run test:discovery` | ✅ discovery tests passed |
| `npm run typecheck` | ✅ tsc --noEmit 零错误 |
| `npm run lint` | ✅ No ESLint warnings or errors |
| 全新数据库初始化（临时目录模拟） | ✅ 112 items / 112 unique ids（修复前抛 UNIQUE constraint failed） |
| seed 唯一性（npx tsx） | ✅ 103 目录条目 + 9 baseSeed = 112，0 重复 |
| CLI 注入验证（npx tsx） | ✅ `--model "gpt-5.1 & calc & rem"` 被 `CLI_UNSAFE_ARGUMENT` 同步拒绝，不产生进程 |
| CLI 缺失场景 | ✅ `spawn ... ENOENT` 被优雅处理，进程不崩（EPIPE 修复生效） |

> 注：原报"7 个测试 4 过 3 挂"在本地现有数据库状态下无法直接复现（旧库已有数据使 `seedIfEmpty` 短路）。本次通过全新数据库环境复现并修复，修复后干净环境与现有环境均通过。
